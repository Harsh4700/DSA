s="this is a word string having many many word"
k=2
word=s.split()
def printKFreqword(s,k):
    word=s.split()
    d={}
    for w in word:
        d[w]=d.get(w,0)+1
    for w in d:
        if d[w]==k:
            print(w)

printKFreqword(s,1)
