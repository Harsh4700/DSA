def remove_x(string):
    if len(string) == 0:  # base case
        return string
    elif string[0] == 'x': 
        return remove_x(string[1:]) 
    else:
        return string[0] + remove_x(string[1:])  

remove_x('n')
#'n'
